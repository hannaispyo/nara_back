{
  "name": "naraback",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "NARA BACK — one-pager estático + CMS (Vercel Postgres + Blob + auth propia)",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "hash-password": "node scripts/hash-password.mjs"
  },
  "dependencies": {
    "@vercel/blob": "^0.27.0",
    "@vercel/postgres": "^0.10.0"
  }
}
